# 📐 Environment Configuration - Worktree-Forms
# Copy this file to .env and update values for your environment
#
# 🚀 PRODUCTION (Dokploy): Use Docker service names (minio, db-service-name)
# 💻 LOCAL DEV: Use external IPs and ports (see MinIO section below)

# --- App Core ---
NODE_ENV=production  # or "development" for local

# Frontend Port (Main Entry)
PORT=3100

# Backend Port (Internal)
BACKEND_PORT=5100

# Bind to all interfaces (required for Docker)
HOSTNAME=0.0.0.0

# Public URL of the application
NEXT_PUBLIC_APP_URL=https://worktree.pro

# --- Database ---
# Use Docker service name for internal communication
# Format: postgresql://user:password@service-name:port/database
DATABASE_URL=postgresql://user:password@database-service:5432/dbname

# --- Security ---
JWT_SECRET=your_32_character_secret_key_here
JWT_EXPIRE=15m
JWT_REFRESH_EXPIRE=7d

# --- Object Storage (MinIO) ---
# ⚠️ MinIO runs as EXTERNAL service (NOT in Docker network)
# Use direct IP for both local dev AND production

# 🚀 PRODUCTION & LOCAL DEV: Use external IP to access MinIO API (port 9004)
MINIO_HOST=67.222.144.10      # External MinIO IP
MINIO_PORT=9004               # MinIO API port (NOT console port 9002)
MINIO_USE_SSL=false          # HTTP for direct IP connection

# MinIO credentials
MINIO_ACCESS_KEY=your_access_key
MINIO_SECRET_KEY=your_secret_key
MINIO_BUCKET_NAME=worktree
MINIO_REGION=us-east-1

# Public MinIO URL for presigned URLs (browsers need this)
# Same for both production and local dev
MINIO_PUBLIC_URL=https://minio.worktree.pro

# --- Frontend Config ---
# Backend host for Next.js rewrites (internal container communication)
BACKEND_HOST=localhost

# Public API URL for client-side requests
NEXT_PUBLIC_API_URL=https://worktree.pro/api

# Public MinIO URL for client-side image access
NEXT_PUBLIC_MINIO_URL=https://minio.worktree.pro
